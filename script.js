const WORKER_URL = "https://avistore-api.mohammadaminmoradi1133.workers.dev"; const purchaseInput = document.getElementById("purchaseId"); const payButton = document.getElementById("payButton"); const message = document.getElementById("message"); payButton.addEventListener("click", async () => { const purchaseId = purchaseInput.value.trim(); if (!purchaseId) { message.className = "error"; message.textContent = "لطفاً شناسه خرید را وارد کنید."; return; } payButton.disabled = true; message.className = ""; message.textContent = "در حال ایجاد تراکنش..."; try { const response = await fetch( WORKER_URL + "/api/create-payment", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ purchase_id: purchaseId }) } ); const data = await response.json(); if ( !response.ok || !data.ok ) { throw new Error( data.error || "ایجاد پرداخت ناموفق بود." ); } message.textContent = "در حال انتقال به زرین‌پال..."; /* * انتقال کاربر به درگاه */ window.location.href = data.url; } catch (error) { console.error(error); message.className = "error"; message.textContent = error.message || "خطایی رخ داد."; payButton.disabled = false; } }); 